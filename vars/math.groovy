 def sum(a,b)
 {
	echo "the sum of ${a} and ${b} is ${a+b}"	
 }
 
 def sub(a,b)
 {
	echo "the substraction res of ${a} and ${b} is ${a-b}"	
 }
 
 def mul(a,b)
 {
	echo "the multipl res of ${a} and ${b} is ${a*b}"
 }
 
 def div(a,b)
 {
	echo "the div res of ${a} and ${b} is ${a/b}"
 }
